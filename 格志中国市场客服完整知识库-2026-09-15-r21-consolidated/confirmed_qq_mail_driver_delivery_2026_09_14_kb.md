# QQ邮箱复用大附件发送Windows通用驱动（2026-09-14确认）

## 1. 驱动身份

- `DotNobleDriver`和`ThermalNobleDriver`是同一个Windows通用打印机驱动安装包的两个名称。
- 通用的是安装包；安装后仍须按实际产品线、型号和连接方式选择正确设备与设置。
- QQ邮箱中已上传的大附件文件名为`DotNobleDriver (2).zip`。客户说要`ThermalNobleDriver`时，内部仍使用这个已确认的同一文件；普通邮件正文不要主动解释两个名称，只有客户明确询问包名身份时才说明。
- 本流程只用于已支持打印机的Windows电脑。Mac或其他系统不得套用此Windows大附件流程。

## 2. 复用原则

- 先在QQ邮箱“已发送”中查找仍在有效期内的`DotNobleDriver`大附件；找到后可在新的客户邮件中复用，无需重复上传。
- 每次都创建一封全新的邮件，只复用已验证的大附件下载能力。不得复制原邮件的收件人、抄送人、主题、正文、历史对话或其他客户信息。
- 大附件的私有下载地址和校验标识只允许在发送时从邮箱实时读取，不得写入知识文档、RAG卡片、索引、报告或客服对话。
- 同一大附件至少还须有72小时有效期。已过期或剩余不足72小时的附件不得继续发送；此时应提示重新在QQ邮箱上传一次，后续再复用新的有效附件。

## 3. 客服发送流程

1. 确认客户需要打印机驱动，并只收集客户邮箱和电脑系统；Windows确认后进入本流程。
2. 使用只读查询查找当前有效的`DotNobleDriver`可复用大附件。
3. 如果客户说的是`ThermalNobleDriver`，内部使用已确认的同一文件，不另外选择或编造第二个文件；普通邮件正文不主动说明两个名称，除非客户明确询问。
4. 发送前向操作人完整显示收件人、主题、正文、文件名和有效期，并取得明确确认。
5. 确认后发送一封全新邮件；一次发送失败时不得自动重试，先报告失败状态，重新确认后才能再次发送。
6. 发送成功只记录安全元数据，例如文件名、显示大小、有效期和发送结果；不得记录私有下载地址。

## 4. 停止与转人工边界

- 客户邮箱格式无效、电脑系统未确认、客户使用Mac、附件不存在、附件身份不匹配、有效期不足72小时或发送失败时，不得猜测或静默替换文件。
- 当前有效附件不足时，可由操作人在QQ邮箱重新上传一次正确文件；完成后系统重新只读查询并显示新的有效期。
- 未经明确确认，不得发送邮件、增加收件人、改变附件或自动重试。

## 5. 推荐邮件正文

> 您好，邮件中提供的是NOBLE Windows通用打印机驱动。请在附件有效期内下载并解压。将打印机接通电源并开机，再用USB数据线连接电脑；安装前请关闭杀毒和防护软件，然后运行安装程序。程序中选择“USB”，点击“搜索”，选择您的打印机型号，再点击“下一步”自动安装。完成后可在Windows设置或打印机属性中点击“打印测试页”。如安装中出现问题，请回复本邮件并提供打印机型号和电脑系统版本。
